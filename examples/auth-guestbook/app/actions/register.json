{
  "app_id": "auth-guestbook",
  "state_id": "auth-guestbook:register:2",
  "state_version": 1,
  "response_mode": "page",
  "blocks": ["register_status", "register"],
  "actions": [
    {
      "id": "open_login",
      "label": "Back to Sign In",
      "verb": "navigate",
      "transport": { "method": "GET" },
      "target": "/login",
      "input_schema": {
        "type": "object",
        "properties": {},
        "additionalProperties": false
      }
    },
    {
      "id": "register",
      "label": "Create Account",
      "verb": "write",
      "transport": { "method": "POST" },
      "target": "/auth/register",
      "input_schema": {
        "type": "object",
        "required": ["username", "password"],
        "properties": {
          "username": { "type": "string" },
          "password": { "type": "string", "format": "password" }
        },
        "additionalProperties": false
      }
    }
  ],
  "allowed_next_actions": ["register", "open_login"]
}
