# __PROJECT_NAME__

Node MDAN starter generated by `create-mdan`.

The app serves human-friendly HTML for browsers and Markdown artifacts for agents.
Action submissions use JSON request bodies and return Markdown artifacts.

```bash
npm install
npm start
```

Open `http://127.0.0.1:4321/`.
